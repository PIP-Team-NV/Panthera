import { MockMetadaBindingDirective } from './mock-metada-binding.directive';

describe('MockMetadaBindingDirective', () => {
  it('should create an instance', () => {
    const directive = new MockMetadaBindingDirective();
    expect(directive).toBeTruthy();
  });
});
