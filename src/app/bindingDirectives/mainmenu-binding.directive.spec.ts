import { MainmenuBindingDirective } from './mainmenu-binding.directive';

describe('MainmenuBindingDirective', () => {
  it('should create an instance', () => {
    // const directive = new MainmenuBindingDirective();
    // expect(directive).toBeTruthy();
  });
});
