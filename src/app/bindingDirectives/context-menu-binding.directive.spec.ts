import { ContextMenuBindingDirective } from './context-menu-binding.directive';

describe('ContextMenuBindingDirective', () => {
  it('should create an instance', () => {
    // const directive = new ContextMenuBindingDirective();
    // expect(directive).toBeTruthy();
  });
});
