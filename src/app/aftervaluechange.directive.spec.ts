import { AftervaluechangeDirective } from './aftervaluechange.directive';

describe('AftervaluechangeDirective', () => {
  it('should create an instance', () => {
    const directive = new AftervaluechangeDirective();
    expect(directive).toBeTruthy();
  });
});
