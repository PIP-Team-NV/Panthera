import { MetadataBindingDirective } from './metadata-binding.directive';

describe('MetadataBindingDirective', () => {
  // it('should create an instance', () => {
  //   const directive = new MetadataBindingDirective();
  //   expect(directive).toBeTruthy();
  // });
});
