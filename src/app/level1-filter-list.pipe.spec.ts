import { Level1FilterListPipe } from './level1-filter-list.pipe';

describe('Level1FilterListPipe', () => {
  it('create an instance', () => {
    const pipe = new Level1FilterListPipe();
    expect(pipe).toBeTruthy();
  });
});
