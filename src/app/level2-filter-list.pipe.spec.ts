import { Level2FilterListPipe } from './level2-filter-list.pipe';

describe('Level2FilterListPipe', () => {
  it('create an instance', () => {
    const pipe = new Level2FilterListPipe();
    expect(pipe).toBeTruthy();
  });
});
